# Homebrew Tap for Agentastic.dev

## Install

```bash
brew install agentasticai/tap/dev
```

Or:

```bash
brew tap agentasticai/tap
brew install dev
```

## About

[Agentastic.dev](https://www.agentastic.dev) is a native macOS code editor. Requires macOS 14 (Sonoma) or later.
